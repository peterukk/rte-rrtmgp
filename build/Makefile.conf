
# GFORTRAN
export FC = gfortran
# Fortran compiler flags
#export FCFLAGS = -O3 -ffree-line-length-none -m64 -march=native -std=f2008 -fno-stack-arrays
export FCFLAGS = -O3 -ffree-line-length-none -m64 -std=f2018 -march=native --fast-math -funroll-loops -ftree-loop-linear -fprefetch-loop-arrays

# DEBUGGING
# export FCFLAGS = -ffree-line-length-none -m64 -std=f2008 -march=native          -pedantic -g -fbounds-check -Wall -fbacktrace -finit-real=nan


export TIME_DIR = /home/peter/gptl-gfortran/
#export TIME_DIR = /data/puk/soft/gptl/


# INTEL
#export FC = ifort
#export FCFLAGS = -O3 -mkl=sequential -heap-arrays -I${MKLROOT}/include
#export FCFLAGS = -O3 -mkl=sequential  -I${MKLROOT}/include
#export FCFLAGS = -O0 -g -traceback -check all -warn interface -fp-stack-check -mkl -I${MKLROOT}/include
# export TIME_DIR = /home/puk/gptl-ifort/

# PGI GPU
# export FC = pgfortran
# export FCFLAGS = -O3 -g  -Mallocatable=03 -ta=tesla:cc60 -Minfo -Mpreprocess -Mcuda -Mcudalib=cublas