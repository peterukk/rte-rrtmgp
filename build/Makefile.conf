
# # Gfortran 
#export FC = gfortran
#export FCFLAGS = -O3 -ffree-line-length-none -m64 -march=native -std=f2008 -fno-stack-arrays
#export FCFLAGS = -O3 -ffree-line-length-none -m64 -march=native -std=f2008 -pg
# export FCFLAGS = -O3 -ffree-line-length-none -m64 -std=f2008 -march=native --fast-math -funroll-loops -ftree-loop-linear -fprefetch-loop-arrays
# # # export FCFLAGS = -O1 -ffree-line-length-none -m64 -std=f2008 -march=native -fno-stack-arrays -fexternal-blas --fast-math
# # #export FCFLAGS = -O3 -ffree-line-length-none -std=f2008 -pg -march=native --fast-math 
#export NCHOME = /usr/
#export NFHOME = /usr/
#export TIME_DIR = /data/puk/soft/gptl/

# Gfortran + MKL
# export FC = gfortran
# export FCFLAGS =  -O3 -ffree-line-length-none -m64 -std=f2008 -march=native --fast-math -funroll-loops -ftree-loop-linear -fprefetch-loop-arrays -I${MKLROOT}/include #  
# export LIBS =  -L${MKLROOT}/lib/intel64 -Wl,--no-as-needed -lmkl_intel_lp64 -lmkl_intel_thread -lmkl_core -liomp5 -lpthread -lm -ldl


# Fortran compiler flags
#
# Optimized
#
#export FCFLAGS = -O3 -fexternal-blas 
#export FCFLAGS = -ffree-line-length-none -m64 -std=f2003 -march=native -O3 

# Debugging - -mno-avx is particular to (broken) gcc setups using MacPorts
#
#export FCFLAGS = -ffree-line-length-none -m64 -std=f2003 -march=native -mno-avx -pedantic -g -fbounds-check -Wall -fbacktrace -finit-real=nan
#export FCFLAGS = -ffree-line-length-none -m64 -std=f2003 -march=native          -pedantic -g -fbounds-check -Wall -fbacktrace -finit-real=nan


# INTEL
export FC = ifort
#export FCFLAGS = -O3 -mkl=sequential -heap-arrays -I${MKLROOT}/include
#export FCFLAGS = -O0 -debug -traceback -check -ftrapuv -heap-arrays -I${MKLROOT}/include
#export FCFLAGS = -O3 -mkl=sequential  -p -save-temps  -I${MKLROOT}/include
export FCFLAGS = -O2 -mkl=sequential -p -save-temps  -I${MKLROOT}/include
#export FCFLAGS = -O0 -g -traceback -check all -warn interface -fp-stack-check -mkl -I${MKLROOT}/include
export LIBS = -L${MKLROOT}/lib/intel64 -lmkl_rt -lpthread -lm -ldl
# # intel compiled netCDF libraries 
# export NCHOME = /home/pepe/netcdf/
# export NFHOME = /home/pepe/netcdf

#INTEL
# export FC = ifort
# export FCFLAGS = -O2 -mkl -heap-arrays -I${MKLROOT}/include
# export LIBS = -L${MKLROOT}/lib/intel64 -lmkl_rt -lpthread -lm -ldl


# export FC = ifort
# export FCFLAGS = -O3 -fp-model source -qopt-matmul -mkl -I${MKLROOT}/include
# export LIBS = -L${MKLROOT}/lib/intel64 -lmkl_rt -lpthread -lm -ldl

# export FC = ifort
# export FCFLAGS = -O3 -mkl -I${MKLROOT}/include
# export LIBS = -L${MKLROOT}/lib/intel64 -lmkl_rt -lpthread -lm -ldl

# export FC = ifort
#export FCFLAGS = -O0 -g -traceback -check all -fp-stack-check -mkl -I${MKLROOT}/include
# export LIBS = -L${MKLROOT}/lib/intel64 -lmkl_rt -lpthread -lm -ldl


# export FC = ifort
# export FCFLAGS = -Ofast -mkl -static-intel
