
# Gfortran 
export FC = gfortran
#export FCFLAGS = -ffree-line-length-none -m64 -std=f2008 -march=native -O3 --fast-math -funroll-loops -ftree-loop-linear -fprefetch-loop-arrays
export FCFLAGS = -ffree-line-length-none -m64 -std=f2008 -march=native -O3 --fast-math 
export LIBS = -lopenblas

# Gfortran + MKL
#export FC = gfortran
#export FCFLAGS = -ffree-line-length-none -m64 -std=f2008 -march=native -O3 -I${MKLROOT}/include #  --fast-math 
#export LIBS =  -L${MKLROOT}/lib/intel64 -Wl,--no-as-needed -lmkl_rt -lpthread -lm -ldl
#export LIBS =  -L${MKLROOT}/lib/intel64 -L${MKLROOT}/lib/ -lmkl_core -lmkl_gnu_thread -lmkl_gf_lp64

# Fortran compiler flags
#
# Optimized
#
#export FCFLAGS = -O3 -fexternal-blas 
#export FCFLAGS = -ffree-line-length-none -m64 -std=f2003 -march=native -O3 

# Debugging - -mno-avx is particular to (broken) gcc setups using MacPorts
#
#export FCFLAGS = -ffree-line-length-none -m64 -std=f2003 -march=native -mno-avx -pedantic -g -fbounds-check -Wall -fbacktrace -finit-real=nan
#export FCFLAGS = -ffree-line-length-none -m64 -std=f2003 -march=native          -pedantic -g -fbounds-check -Wall -fbacktrace -finit-real=nan


# INTEL

#export FC = ifort
#export FCFLAGS = -O3 -qopt-matmul -mkl -I${MKLROOT}/include
#export LIBS = -L${MKLROOT}/lib/intel64 -lmkl_rt -lpthread -lm -ldl

